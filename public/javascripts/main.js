
// ------------------ event listeners --------------------//

document.querySelector('body').addEventListener('click', handleClick);

// ------------------ functions --------------------------//

function handleClick(evt) {
    if(evt.target.id === 'dest-button') {
    
    }
}